{
  "name": "school-notes",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "grunt build",
    "start": "cd dist && python3 -m http.server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Seah Chin Ying",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-contrib-copy": "^1.0.0"
  }
}
